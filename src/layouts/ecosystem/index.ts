/**
 * Ecosystem layout — cross-domain composition surface (Phase 3 placeholder).
 * Future: multi-pane layout that hosts Espace + Talents + Wallet side-by-side
 * on large screens.
 */
export { AppShell as EcosystemLayout } from "@/components/app/AppShell";
