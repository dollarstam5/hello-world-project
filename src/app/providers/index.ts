/**
 * Application-wide providers (theme, query client, i18n, etc.).
 * Phase 1: theme bootstrap lives in src/lib/app/theme.ts and is re-exported here.
 */
export { initTheme, useThemeStore, type ThemeMode } from "@/lib/app/theme";
