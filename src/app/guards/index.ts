/**
 * Route guards (auth, feature flags, permissions).
 * Phase 1 placeholder — wire concrete guards in later phases.
 */
export { isEnabled, features, type FeatureKey } from "@/lib/app/featureFlags";
